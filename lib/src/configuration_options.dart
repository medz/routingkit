/// Available configuration options for [TrieRouter].
class ConfigurationOptions {
  /// Whether to use case sensitive matching.
  final bool caseSensitive;

  const ConfigurationOptions({
    this.caseSensitive = true,
  });
}
