name: routingkit
description: >-
  High-performance trie-node router.
  It is based on Trie-Node and supports dynamic, static, parameter, wildcard routing, etc.
version: 0.1.2
repository: https://github.com/medz/routingkit

funding:
  - https://github.com/sponsors/medz

topics:
  - routing
  - router
  - routing-kit
  - trie
  - trie-node

environment:
  sdk: '>=3.0.0 <3.3.0'

dependencies:
  logging: ^1.2.0

dev_dependencies:
  benchmark_harness: ^2.2.2
  lints: ^3.0.0
  test: ^1.24.0
