name: routingkit
description: High-performance trie-node router.
version: 0.0.1
repository: https://github.com/medz/trie-router

environment:
  sdk: ^3.2.3

# Add regular dependencies here.
dependencies:
  logging: ^1.2.0

dev_dependencies:
  benchmark_harness: ^2.2.2
  lints: ^2.1.0
  test: ^1.24.0
